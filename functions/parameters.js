function greeting(userName) {
 console.log(`Hi, ${userName}.`);
}
greeting('Summer');

greeting('michele');

// /* 1: the parameter(s) that the funtion is accepting or holding. 
// // 2: using string interpolation, we can refer to the parameter given to the function
// // 3: this si where we define what the parameter's 

// this is correct, but I didnt use a variable....like the questions required boo!
function theName(firstName, lastName) {
    console.log(`hello, my name is ${firstName}${lastName}.`);
}

theName('Michele ', 'Countryman');